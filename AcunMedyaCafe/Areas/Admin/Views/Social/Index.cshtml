@model List<AcunMedyaCafe.Entities.Social>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    var count = 0;
}

<div class="content-wrapper">
    <div class="page-header">
        <h3 class="page-title"> Sosyal Medya Tablosu </h3>
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="#">Sosyal Medya Listesi</a></li>
                <li class="breadcrumb-item active" aria-current="page">Sosyal Medya</li>
            </ol>
        </nav>
    </div>
    <div class="row">
        <div class="col-lg-12 grid-margin stretch-card">
            <div class="card">
                <div class="card-body">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Logo</th>
                                <th>Platform</th>
                                <th>Link</th>
                                <th>Güncelle</th>
                                <th>Sil</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                count++;
                                <tr>
                                    <td>@count</td>
                                    <td><img src="@item.ImageUrl" style="width:50px; height:50px;" /></td>
                                    <td>@item.Platform</td>
                                    <td><a href="@item.AccountUrl" class="badge badge-warning">Hesap</a></td>
                                    <td><a href="/Admin/Social/UpdateSocial/@item.SocialId" class="badge badge-info">Güncelle</a></td>
                                    <td><a onclick="deleteSocial(@item.SocialId)" class="badge badge-danger">Sil</a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                    <a href="/Admin/Social/AddSocial" class="btn btn-primary">Yeni Hesap Ekle</a>
                </div>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<script>
    function deleteSocial(id){
        Swal.fire({
            title: 'Silmek İstediğinize Emin Misiniz?',
            text: "Bu işlem geri alınamaz!",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#3085d6',
            cancelButtonColor: '#d33',
            confirmButtonText: 'Evet, Sil!',
            cancelButtonText: 'İptal Et!'
        }).then((result) => {
            if (result.isConfirmed) {
                window.location.href = "/Admin/Social/DeleteSocial/" + id;
            }
        })
    }
</script>





